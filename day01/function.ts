// add 함수 만들기 a,b 둘 다 숫자여야함

const add = (a: number, b: number): number => {
  return a + b;
};

// minus, multiply 함수 만들기 ts로 컴파일까지
const minus = (a: number, b: number): number => {
  return a - b;
};

const multiply = (a: number, b: number): number => {
  return a * b;
};
